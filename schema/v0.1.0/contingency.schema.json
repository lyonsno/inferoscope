{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://inferoscope.local/schema/v0.1.0/contingency.schema.json",
  "title": "inferoscope contingency v0.1.0 provisional",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "derivation_version",
    "contingency_payload"
  ],
  "properties": {
    "schema_version": {
      "const": "contingency/v0.1.0-provisional"
    },
    "run_id": {
      "type": "string",
      "minLength": 1
    },
    "derivation_version": {
      "type": "string",
      "minLength": 1
    },
    "contingency_payload": {
      "type": "object",
      "description": "Provisional contingency payload. Internal fields are intentionally not fixed in v0.1.0."
    }
  }
}
